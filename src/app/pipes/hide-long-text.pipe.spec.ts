import { HideLongTextPipe } from './hide-long-text.pipe';

describe('HideLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new HideLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
